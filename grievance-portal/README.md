# Frontend (Grievance Portal)

This folder contains the React + Vite frontend for the Grievance Portal.

For complete setup and run instructions (frontend + backend + Docker), use:

- `../README.md`

